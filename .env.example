# Copiar a .env y ajustar a tu entorno.
# URL de conexion PostgreSQL (driver psycopg3).
# Formato: postgresql+psycopg://[user[:password]@][host][:port]/db
IMDB_DB_URL=postgresql+psycopg://localhost/imdb_db
